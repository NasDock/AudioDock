# 2026-09-13 工作日志

## mobile 版本更新统一走 APK 直装（不再区分平台）

- 需求：mobile 端不再按设备品牌分流，统一走小米的版本更新逻辑（系统下载器下载 APK）；iOS 滞后比较久，暂时不管
- 改动：
  - `apps/mobile/hooks/useCheckUpdate.ts`：删 `isXiaomiDevice` 分支，删 `openStoreByPlatform`/`getCurrentStoreUrl` import；`UpdateInfo` 接口删 `mode` 和 `storeUrl`，统一保留 `downloadUrl: string`；`checkUpdate` 永远只从后端取 `platform='android'` 的 APK URL，取不到就 return null（不再跳商店兜底）；`startUpdate` 统一调 `downloadAndInstallApk`
  - `apps/mobile/src/components/UpdateModal.tsx`：删 `mode === 'xiaomi'` 判断（按钮文案、下载视图判定）；按钮文案统一 `t('update.updateNow')`；下载视图简化为 `isDownloading = opening`
  - `packages/i18e/src/locales/common/zh-CN.json` + `en.json`：新增 `update.downloadApkFailedTitle/Body`（"更新下载失败"），错误提示用新 key
  - `apps/mobile/src/services/openStore.ts` + `constants/store.ts`：更新注释，标注这些常量/函数不再被版本更新流程调用，仅保留给 `openStoreDebug`（个人中心调试按钮）和后续兜底
- iOS 行为：iOS 上 `checkUpdate` 仍会调后端拿 APK URL；`startUpdate` 调 `downloadAndInstallApk`，但 `updateUtils.downloadAndInstallApk` 在 iOS 上 noop + warn，弹窗关闭即视为"暂无 iOS 版本"
- 验证：`tsc --noEmit` 改动文件 0 error；个人中心 `openStoreDebug` 按钮未受影响（仍用 openStore.ts 的 `IOS_APP_STORE_URL` 和 `market://` 协议）
- 注意：dead code（`getCurrentStoreUrl`/`openStoreByPlatform`/`probeAndroidStore`/`getAndroidStoreKey`/`ANDROID_STORE_ORDER`）先保留，等用户确认是否清掉