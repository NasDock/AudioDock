# 2026-09-10

## 修复 HarmonyOS 编译报错（111 errors）

- 根因 1：`apps/harmony/products/entry/src/main/ets/services/WidgetBridge.ets` 有 6 处代码被意外截断/合并（`resolveCoverToFile` 方法体挤到一行、模板字符串被截断等），导致 ArkTS 编译器解析失败并产生 100+ 连锁报错。
- 根因 2：`apps/harmony/products/entry/src/main/ets/services/trackQuality.ets` 使用了 ArkTS 不存在的全局 `URL` 类。
- 修复：
  - `WidgetBridge.ets` 6 处全部还原，`git diff` 已为空。
  - `trackQuality.ets` 用手写 `extractHost()` 替代 `new URL()`。
- 验证：`assembleHap` BUILD SUCCESSFUL，错误清零。
